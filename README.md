# Contact-Manager
Full-stack application that allows users to upload a CSV file with the following fields, like name date of birth, salary etc. Store the data into MS SQL database, and display stored data on the page
